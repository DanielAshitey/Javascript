let currentAge = 21; maxAge = 100; calsperday = 3000;
let lifeAmount = calsperday*(maxAge - currentAge);

console.log("You will need " + lifeAmount + " calories to survive until the age of " + maxAge)
